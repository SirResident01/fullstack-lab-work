# Отчёт по лабораторным работам №10 и №11

## Цель
Реализовать одностраничное приложение «Car Shop», в котором фронтенд на React+TS (Next.js) взаимодействует с REST‑бекендом (FastAPI). В ЛР №10 требовалось подготовить окружение, подключить основные библиотеки и убедиться в работе связки фронт/бекенд. В ЛР №11 нужно было вывести список автомобилей с расширенными возможностями (фильтрация, сортировка, пагинация), CRUD‑операциями и экспортом данных.

## Среда и инструменты
- Backend: FastAPI + SQLAlchemy + SQLite, запуск через `uvicorn`.
- Frontend: Next.js 14, TypeScript, React Query, Axios, Tailwind CSS (собственная библиотека UI‑компонентов).
- Доп. библиотеки: react-hook-form, clsx, tailwind-merge.

## Лабораторная работа №10

### Backend
- Поднято FastAPI-приложение (`app/main.py`) без Spring Security. Авторизация реализована JWT-токенами (HTTP Bearer).
- Основной эндпоинт `/cars` отдаёт список автомобилей с информацией о владельцах.
- Настроен CORS для localhost-портов, что позволяет фронтенду обращаться к API.

### Frontend
- Создан Next.js-проект с TypeScript. Установлены React Query, Axios и вспомогательные библиотеки.
- В `lib/api.ts` настроен универсальный `axios`-клиент с базовым URL и перехватчиками запросов/ответов.
- На главной странице (`pages/index.tsx`) реализован AppBar со статусом сервиса, навигационными карточками и проверкой подключения к API (`useQuery('status', apiClient.getStatus)`).
- Проверено, что `npm run dev` запускает фронтенд, а все запросы успешно уходят на FastAPI.

## Лабораторная работа №11

### Получение и отображение данных
- Страница `pages/cars/index.tsx` использует `useQuery(['cars', searchQuery], () => apiClient.searchCars(searchQuery))` для загрузки списка машин с учётом фильтров.
- Компонент `SearchFilters` управляет фильтрами (`brand`, `color`, `modelYear`, цена, сортировка, limit/offset), тем самым реализуя функционал сортировки, фильтрации и пагинации.
- Для альтернативного упрощённого вида существует страница `pages/cars/simple.tsx` с теми же данными.

### CRUD-операции
- Создание, обновление и удаление выполняются через `useMutation` (`createCar`, `updateCar`, `deleteCar`). После успешной операции инвалидируется кэш `cars`, поэтому список обновляется автоматически.
- Формы добавления/редактирования реализованы в `components/cars/CarForm.tsx` (react-hook-form + модальное окно).
- Удаление подтверждается отдельной модалкой.
- Действия доступны только пользователям с ролью `ADMIN` (проверка в `useAuth`).

### Экспорт CSV
- В шапку обеих страниц добавлена кнопка «📤 Экспорт CSV». При нажатии формируется CSV-файл с учётом локали (`,` или `;`), данные экранируются и очищаются от переводов строк, что гарантирует корректное открытие в Excel.

### Дополнительные детали
- В `components/cars/CarCard.tsx` карточки показывают популярные свойства авто; цены форматируются функцией `formatPrice`.
- На главной странице отображается статистика по автомобилям и владельцам (данные из `/cars/statistics` и `/owners/statistics`).

## Тестирование
- Проверка ручным сценариям:
  - Запуск backend (`uvicorn app.main:app --reload`) и frontend (`npm run dev`), авторизация под администратором.
  - Создание/редактирование/удаление машины с последующим обновлением списка.
  - Применение фильтров (марка, цвет, ценовой диапазон) и наблюдение за обновлением результатов.
  - Экспорт CSV и открытие файла в Excel (значения корректно попадают в столбцы).

## Вывод
Обе лабораторные работы выполнены: настроена полная связка FastAPI + React Query, реализованы все CRUD-функции для автомобилей, динамические фильтры и экспорт данных в CSV. Приложение готово к демонстрации и дальнейшему расширению.




