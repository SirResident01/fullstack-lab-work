# 📖 Подробная документация Lab #1

## 🎯 Цель проекта

Создание веб-API для управления базой данных автомобилей и их владельцев с использованием современных технологий Python.

## 🏗️ Архитектура проекта

### Технологический стек:
- **Backend**: Python 3.11+ + FastAPI
- **База данных**: PostgreSQL 16+
- **ORM**: SQLAlchemy 2.x
- **Валидация**: Pydantic 2.x
- **Сервер**: Uvicorn
- **Управление БД**: pgAdmin

### Принципы архитектуры:
- **Модульность** — код разделен на логические модули
- **Типизация** — полная поддержка типов Python
- **Валидация** — автоматическая валидация входных и выходных данных
- **Документация** — автоматическая генерация API документации

## 📊 Модель данных

### Сущность "Владелец" (Owner):
```python
class Owner(Base):
    __tablename__ = "owner"
    ownerid: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    firstname: Mapped[str] = mapped_column(String(100))
    lastname: Mapped[str] = mapped_column(String(100))
    cars: Mapped[list["Car"]] = relationship(back_populates="owner", cascade="all, delete-orphan")
```

**Описание полей:**
- `ownerid` — уникальный идентификатор владельца
- `firstname` — имя владельца (максимум 100 символов)
- `lastname` — фамилия владельца (максимум 100 символов)
- `cars` — связь с автомобилями (один ко многим)

### Сущность "Автомобиль" (Car):
```python
class Car(Base):
    __tablename__ = "car"
    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    brand: Mapped[str] = mapped_column(String(100))
    model: Mapped[str] = mapped_column(String(100))
    color: Mapped[str] = mapped_column(String(40))
    registrationNumber: Mapped[str] = mapped_column(String(40))
    modelYear: Mapped[int] = mapped_column(Integer)
    price: Mapped[int] = mapped_column(Integer)
    owner_id: Mapped[int] = mapped_column(ForeignKey("owner.ownerid"))
    owner: Mapped["Owner"] = relationship(back_populates="cars")
```

**Описание полей:**
- `id` — уникальный идентификатор автомобиля
- `brand` — марка автомобиля (максимум 100 символов)
- `model` — модель автомобиля (максимум 100 символов)
- `color` — цвет автомобиля (максимум 40 символов)
- `registrationNumber` — регистрационный номер (максимум 40 символов)
- `modelYear` — год выпуска (целое число)
- `price` — цена в рублях (целое число)
- `owner_id` — внешний ключ на владельца
- `owner` — связь с владельцем

## 🔌 API Эндпоинты

### 1. GET /hello
**Назначение**: Простое приветствие для проверки работоспособности сервера

**Запрос:**
```http
GET /hello
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: text/plain; charset=utf-8

Hello from FastAPI!
```

### 2. GET /api/status
**Назначение**: Получение статуса приложения и информации о версии

**Запрос:**
```http
GET /api/status
```

**Ответ:**
```json
{
  "status": "ok",
  "app": "Lab1 FastAPI",
  "version": "1.0.0"
}
```

**Схема ответа (Pydantic):**
```python
class Status(BaseModel):
    status: str
    app: str
    version: str
```

### 3. GET /cars
**Назначение**: Получение списка всех автомобилей с информацией о владельцах

**Запрос:**
```http
GET /cars
```

**Ответ:**
```json
[
  {
    "id": 1,
    "brand": "Ford",
    "model": "Mustang",
    "owner": "John Johnson"
  },
  {
    "id": 2,
    "brand": "Nissan",
    "model": "Leaf",
    "owner": "Mary Robinson"
  },
  {
    "id": 3,
    "brand": "Toyota",
    "model": "Prius",
    "owner": "Mary Robinson"
  }
]
```

**Схема ответа (Pydantic):**
```python
class CarOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: int
    brand: str
    model: str
    owner: Optional[str] = None
```

## 🗄️ Работа с базой данных

### Подключение к PostgreSQL:
```python
# app/db.py
DB_URL = os.getenv("DB_URL", "postgresql+psycopg://postgres:postgres@localhost:5432/cardb")
engine = create_engine(DB_URL, echo=True, pool_pre_ping=True)
SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)
```

### Инициализация и заполнение данными:
```python
def init_db_with_seed() -> None:
    """Создание таблиц и заполнение тестовыми данными"""
    Base.metadata.create_all(engine)
    with SessionLocal() as s:
        if not s.query(Owner).first():  # Проверяем, есть ли данные
            # Создаем владельцев
            o1 = Owner(firstname="John", lastname="Johnson")
            o2 = Owner(firstname="Mary", lastname="Robinson")
            s.add_all([o1, o2])
            
            # Создаем автомобили
            s.add_all([
                Car(brand="Ford", model="Mustang", color="Red", 
                    registrationNumber="ADF-1121", modelYear=2023, price=59000, owner=o1),
                Car(brand="Nissan", model="Leaf", color="White", 
                    registrationNumber="SSJ-3002", modelYear=2020, price=29000, owner=o2),
                Car(brand="Toyota", model="Prius", color="Silver", 
                    registrationNumber="KKO-0212", modelYear=2022, price=39000, owner=o2),
            ])
            s.commit()
```

## 🔧 Конфигурация

### Переменные окружения (.env):
```env
# Порт сервера
APP_PORT=8000

# URL подключения к PostgreSQL
DB_URL=postgresql+psycopg://postgres:ВАШ_ПАРОЛЬ@localhost:5432/cardb

# Название приложения
APP_NAME=Lab1 FastAPI

# Версия приложения
APP_VERSION=1.0.0
```

### Логирование:
```python
logging.basicConfig(
    level=logging.DEBUG, 
    format="%(levelname)s %(name)s: %(message)s"
)
log = logging.getLogger("lab1")
```

## 🚀 Процесс запуска

### 1. PowerShell скрипт (run.ps1):
```powershell
# Создание виртуального окружения
if (-not (Test-Path .venv)) { py -m venv .venv }

# Активация виртуального окружения
.\.venv\Scripts\Activate.ps1

# Установка зависимостей
pip install -r requirements.txt

# Определение порта из .env или параметра командной строки
$resolvedPort = 8000
if ($Port) { $resolvedPort = $Port }
elseif (Test-Path .env) {
  $line = Get-Content .env | Where-Object { $_ -match '^APP_PORT=' } | Select-Object -First 1
  if ($line) { $resolvedPort = ($line -split '=',2)[1] }
}

# Запуск сервера с автоперезагрузкой
uvicorn app.main:app --reload --port $resolvedPort
```

### 2. Зависимости (requirements.txt):
```
fastapi==0.115.*          # Веб-фреймворк
uvicorn[standard]==0.30.* # ASGI сервер
python-dotenv==1.0.*      # Загрузка .env файлов
sqlalchemy==2.0.*         # ORM для работы с БД
pydantic==2.*             # Валидация данных
psycopg[binary]==3.2.*    # PostgreSQL драйвер
```

## 📝 Тестирование API

### Использование curl:
```bash
# Проверка приветствия
curl http://localhost:8000/hello

# Получение статуса
curl http://localhost:8000/api/status

# Получение списка автомобилей
curl http://localhost:8000/cars
```

### Использование PowerShell:
```powershell
# Проверка приветствия
Invoke-WebRequest -Uri "http://localhost:8000/hello" -UseBasicParsing

# Получение статуса
Invoke-WebRequest -Uri "http://localhost:8000/api/status" -UseBasicParsing

# Получение списка автомобилей
Invoke-WebRequest -Uri "http://localhost:8000/cars" -UseBasicParsing
```

### Использование Swagger UI:
1. Откройте браузер
2. Перейдите на `http://localhost:8000/docs`
3. Используйте интерактивный интерфейс для тестирования API

## 🐛 Отладка и решение проблем

### Проблема: Ошибка подключения к PostgreSQL
**Симптомы:**
```
sqlalchemy.exc.OperationalError: (psycopg.OperationalError) connection to server at "localhost" (127.0.0.1), port 5432 failed
```

**Решение:**
1. Убедитесь, что PostgreSQL запущен
2. Проверьте пароль в `.env` файле
3. Убедитесь, что база данных `cardb` создана
4. Проверьте, что порт 5432 не заблокирован файрволом

### Проблема: Порт уже используется
**Симптомы:**
```
OSError: [Errno 98] Address already in use
```

**Решение:**
```powershell
# Найдите процесс на порту 8000
netstat -ano | findstr :8000

# Завершите процесс
taskkill /PID <PID> /F

# Или используйте другой порт
.\run.ps1 -Port 8001
```

### Проблема: Ошибка установки зависимостей
**Симптомы:**
```
ERROR: Could not find a version that satisfies the requirement
```

**Решение:**
```powershell
# Обновите pip
python -m pip install --upgrade pip

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

## 📈 Возможности расширения

### Добавление новых эндпоинтов:
1. Создайте новые функции в `app/main.py`
2. Добавьте соответствующие Pydantic модели
3. Обновите документацию

### Добавление новых сущностей:
1. Создайте новые модели в `app/models.py`
2. Добавьте связи с существующими моделями
3. Обновите функцию `init_db_with_seed()`

### Добавление аутентификации:
1. Установите `python-jose` и `passlib`
2. Создайте модели пользователей
3. Добавьте middleware для проверки токенов

## 📚 Дополнительные ресурсы

- [Документация FastAPI](https://fastapi.tiangolo.com/)
- [Документация SQLAlchemy](https://docs.sqlalchemy.org/)
- [Документация PostgreSQL](https://www.postgresql.org/docs/)
- [Документация pgAdmin](https://www.pgadmin.org/docs/)

