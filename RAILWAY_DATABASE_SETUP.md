# 🔧 Настройка PostgreSQL на Railway

## Проблема
Приложение пытается подключиться к `localhost:5432`, но на Railway нужно использовать `DATABASE_URL`.

## ✅ Решение (3 шага):

### Шаг 1: Добавить PostgreSQL базу данных

1. В Railway Dashboard откройте ваш проект
2. Нажмите **"+ New"**
3. Выберите **"Database"** → **"Add PostgreSQL"**
4. Railway автоматически создаст базу данных

### Шаг 2: Связать базу данных с вашим сервисом

1. Откройте созданную PostgreSQL базу данных
2. Перейдите в **"Settings"** → **"Connect"**
3. В разделе **"Connect to Service"** выберите ваш **Web Service**
4. Нажмите **"Connect"**

**Важно:** После этого Railway автоматически добавит переменную `DATABASE_URL` в ваш Web Service!

### Шаг 3: Проверить переменные окружения

1. Откройте ваш **Web Service**
2. Перейдите в **"Variables"**
3. Убедитесь, что есть переменная `DATABASE_URL`
4. Значение должно быть примерно таким:
   ```
   postgresql://postgres:password@hostname.railway.app:5432/railway
   ```

## 🔍 Если DATABASE_URL не появилась автоматически:

### Вариант 1: Скопировать Connection String вручную

1. Откройте PostgreSQL базу данных
2. Перейдите в **"Settings"** → **"Connect"**
3. Скопируйте **"Connection String"** (он будет вида `postgresql://...`)
4. В вашем Web Service → **"Variables"** → **"+ New Variable"**
5. Добавьте:
   - **Key:** `DATABASE_URL`
   - **Value:** вставьте скопированный Connection String

### Вариант 2: Использовать Reference Variable

1. В вашем Web Service → **"Variables"** → **"+ New Variable"**
2. Добавьте:
   - **Key:** `DATABASE_URL`
   - **Value:** `${{Postgres.DATABASE_URL}}`
   - (где `Postgres` - это имя вашей PostgreSQL базы данных)

## ✅ После настройки:

1. Railway автоматически перезапустит ваш сервис
2. Проверьте логи - должны увидеть:
   ```
   Database URL: postgresql+psycopg://***:***@hostname.railway.app:5432/railway
   Database tables created/verified
   Application started successfully
   ```

## 🚨 Что НЕ нужно делать:

- ❌ НЕ указывайте `localhost` - это не работает на Railway
- ❌ НЕ указывайте `127.0.0.1` - это не работает на Railway
- ❌ НЕ создавайте переменную вручную с localhost
- ✅ Используйте только `DATABASE_URL`, который создает Railway

## 📝 Дополнительные переменные окружения:

После настройки `DATABASE_URL`, добавьте остальные:

```
SECRET_KEY=your-very-secure-secret-key-min-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
CORS_ORIGINS=https://fullstack-lab-work.vercel.app,https://fullstack-lab-work-oajjvn4s2-llls-projects-d13c13b6.vercel.app
```

## 🎯 Итого:

**Вместо:**
```
postgresql+psycopg://postgres@localhost:5432/cardb
```

**Используйте:**
```
DATABASE_URL (создается автоматически Railway)
```

Значение будет примерно:
```
postgresql://postgres:password@hostname.railway.app:5432/railway
```

Но вы **НЕ указываете это вручную** - Railway делает это автоматически!

