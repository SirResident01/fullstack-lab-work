# ОТЧЕТ ПО ЛАБОРАТОРНОЙ РАБОТЕ №3
## Создание Full-Stack веб-приложения с Next.js и FastAPI

---

## 1. ЦЕЛЬ РАБОТЫ

**Основная цель**: Создание полнофункционального веб-приложения с современным фронтендом на Next.js и мощным бэкендом на FastAPI для управления автомобилями и их владельцами.

**Задачи**:
- Разработка современного пользовательского интерфейса на React/Next.js
- Интеграция фронтенда с REST API
- Реализация полного функционала CRUD операций
- Создание адаптивного и интуитивного интерфейса
- Обеспечение производительности и удобства использования

---

## 2. ТЕХНИЧЕСКОЕ ЗАДАНИЕ

### 2.1 Технологический стек

**Frontend (Next.js)**:
- **Framework**: Next.js 14.0+ с TypeScript
- **Styling**: Tailwind CSS 3.3+ с адаптивным дизайном
- **State Management**: React Query для кэширования и синхронизации данных
- **Forms**: React Hook Form для управления формами
- **HTTP Client**: Axios для API запросов
- **UI Components**: Кастомные компоненты с TypeScript

**Backend (FastAPI)**:
- **Framework**: FastAPI с Python 3.11+
- **Database**: PostgreSQL с SQLAlchemy ORM
- **Validation**: Pydantic v2 для валидации данных
- **CORS**: Настроенная поддержка для фронтенда

### 2.2 Функциональные требования

**Основной функционал**:
- ✅ Управление автомобилями (CRUD операции)
- ✅ Управление владельцами (CRUD операции)
- ✅ Расширенный поиск и фильтрация
- ✅ Статистика и аналитика
- ✅ Адаптивный интерфейс
- ✅ Реальное время обновления данных

**Дополнительные возможности**:
- ✅ Автоматическое обновление данных
- ✅ Кэширование и оптимизация запросов
- ✅ Обработка ошибок и состояний загрузки
- ✅ Модальные окна для форм
- ✅ Подтверждение удаления

---

## 3. АРХИТЕКТУРА ПРОЕКТА

### 3.1 Структура проекта

```
fullstack-lab-work/
├── app/                          # Backend (FastAPI)
│   ├── main.py                   # Основное приложение
│   ├── models.py                 # SQLAlchemy модели
│   ├── schemas.py                # Pydantic схемы
│   ├── crud.py                   # CRUD операции
│   └── db.py                     # Подключение к БД
├── components/                   # Frontend компоненты
│   ├── cars/                     # Компоненты автомобилей
│   ├── owners/                   # Компоненты владельцев
│   ├── search/                   # Компоненты поиска
│   └── ui/                       # UI компоненты
├── pages/                        # Next.js страницы
│   ├── index.tsx                 # Главная страница
│   ├── cars/simple.tsx           # Управление автомобилями
│   ├── owners/simple.tsx         # Управление владельцами
│   └── search/simple.tsx         # Поиск
├── hooks/                        # React хуки
├── lib/                          # Утилиты и API клиент
├── types/                        # TypeScript типы
└── styles/                       # Глобальные стили
```

### 3.2 Архитектурные принципы

**Frontend**:
- **Компонентная архитектура** с переиспользуемыми компонентами
- **Типизация** с TypeScript для безопасности типов
- **Реактивное состояние** с React Query
- **Модульность** с четким разделением ответственности

**Backend**:
- **RESTful API** с четкой структурой эндпоинтов
- **Слоистая архитектура** (контроллеры, сервисы, репозитории)
- **Валидация данных** на всех уровнях
- **Обработка ошибок** с детальными сообщениями

---

## 4. РЕАЛИЗОВАННЫЙ ФУНКЦИОНАЛ

### 4.1 Главная страница (Dashboard)

**Файл**: `pages/index.tsx`

**Функциональность**:
- 📊 **Статистика в реальном времени** - отображение общей статистики по автомобилям и владельцам
- 🔄 **Автообновление** - данные обновляются каждую минуту
- 📱 **Адаптивный дизайн** - корректное отображение на всех устройствах
- 🎯 **Навигация** - быстрый доступ ко всем разделам системы

**Ключевые особенности**:
```typescript
// Автообновление данных
useAutoRefresh(60000); // Каждую минуту

// Кэширование статистики
const { data: carStats } = useQuery(
  'carStatistics',
  () => apiClient.getCarStatistics(),
  {
    staleTime: 5 * 60 * 1000, // 5 минут
    cacheTime: 10 * 60 * 1000, // 10 минут
  }
);
```

### 4.2 Управление автомобилями

**Файл**: `pages/cars/simple.tsx`

**Функциональность**:
- ✅ **Просмотр списка** - отображение всех автомобилей с детальной информацией
- ➕ **Добавление** - модальная форма для создания нового автомобиля
- ✏️ **Редактирование** - модальная форма для изменения данных
- 🗑️ **Удаление** - с подтверждением действия
- 🔄 **Синхронизация** - автоматическое обновление после изменений

**Технические особенности**:
```typescript
// Мутации с автоматической инвалидацией кэша
const createCarMutation = useMutation(
  (carData: CarCreate) => apiClient.createCar(carData),
  {
    onSuccess: () => {
      queryClient.invalidateQueries('cars');
      queryClient.invalidateQueries('carStatistics');
      queryClient.invalidateQueries('ownerStatistics');
    },
  }
);
```

### 4.3 Управление владельцами

**Файл**: `pages/owners/simple.tsx`

**Функциональность**:
- ✅ **CRUD операции** - полный набор операций с владельцами
- 🔗 **Связи с автомобилями** - отображение автомобилей владельца
- 📊 **Статистика** - количество автомобилей у каждого владельца
- 🔍 **Поиск** - быстрый поиск по имени или фамилии

### 4.4 Расширенный поиск

**Файл**: `pages/search/simple.tsx`

**Функциональность**:
- 🔍 **Множественные фильтры** - поиск по марке, цвету, году, цене
- 📊 **Сортировка** - по различным полям в возрастающем/убывающем порядке
- 📄 **Пагинация** - для больших объемов данных
- ⚡ **Производительность** - оптимизированные запросы к API

---

## 5. КОМПОНЕНТНАЯ АРХИТЕКТУРА

### 5.1 UI Компоненты

**Базовые компоненты** (`components/ui/`):
- `Button.tsx` - Универсальная кнопка с вариантами стилей
- `Card.tsx` - Карточка для отображения контента
- `Modal.tsx` - Модальное окно для форм
- `LoadingSpinner.tsx` - Индикатор загрузки
- `Badge.tsx` - Бейдж для меток

**Специализированные компоненты**:
- `CarCard.tsx` - Карточка автомобиля с действиями
- `OwnerCard.tsx` - Карточка владельца
- `SearchFilters.tsx` - Фильтры для поиска

### 5.2 Хуки для управления состоянием

**Файлы в `hooks/`**:
- `useDataRefresh.ts` - Автоматическое обновление данных
- `useAutoRefresh.ts` - Периодическое обновление
- `usePersistedState.ts` - Сохранение состояния в localStorage
- `useSearchState.ts` - Управление состоянием поиска

---

## 6. API ИНТЕГРАЦИЯ

### 6.1 API Клиент

**Файл**: `lib/api.ts`

**Функциональность**:
- 🔗 **Централизованный клиент** - единая точка для всех API запросов
- 📝 **Логирование** - детальное логирование запросов и ответов
- ⚡ **Таймауты** - настройка таймаутов для запросов
- 🔄 **Интерцепторы** - обработка запросов и ответов

```typescript
class ApiClient {
  private client: AxiosInstance;

  constructor() {
    this.client = axios.create({
      baseURL: process.env.NEXT_PUBLIC_API_BASE_URL || 'http://127.0.0.1:8000',
      timeout: 10000,
      headers: { 'Content-Type': 'application/json' },
    });
  }
}
```

### 6.2 Типизация API

**Файл**: `types/api.ts`

**Структура**:
- **CarCreate, CarUpdate, CarResponse** - типы для автомобилей
- **OwnerCreate, OwnerUpdate, OwnerResponse** - типы для владельцев
- **CarQuery, OwnerQuery** - типы для поисковых запросов
- **StatusResponse, MessageResponse** - служебные типы

---

## 7. СТИЛИЗАЦИЯ И ДИЗАЙН

### 7.1 Tailwind CSS

**Конфигурация**:
- **Адаптивный дизайн** - mobile-first подход
- **Цветовая схема** - кастомные цвета для брендинга
- **Компоненты** - переиспользуемые стили
- **Темы** - поддержка светлой/темной темы

### 7.2 UI/UX Особенности

**Дизайн-система**:
- 🎨 **Современный интерфейс** - чистый и интуитивный дизайн
- 📱 **Мобильная адаптация** - корректная работа на всех устройствах
- ⚡ **Быстрые анимации** - плавные переходы и hover-эффекты
- 🎯 **Понятная навигация** - логичная структура меню

**Эмодзи-иконки**:
- 🚗 - Автомобили
- 👥 - Владельцы
- 🔍 - Поиск
- 📊 - Статистика
- ➕ - Добавление
- ✏️ - Редактирование
- 🗑️ - Удаление

---

## 8. ПРОИЗВОДИТЕЛЬНОСТЬ И ОПТИМИЗАЦИЯ

### 8.1 React Query

**Кэширование**:
```typescript
// Настройка кэширования
const { data: cars } = useQuery(
  'cars',
  () => apiClient.getCars(0, 100),
  {
    keepPreviousData: true, // Сохранение предыдущих данных
    staleTime: 5 * 60 * 1000, // 5 минут до устаревания
    cacheTime: 10 * 60 * 1000, // 10 минут в кэше
  }
);
```

**Инвалидация кэша**:
```typescript
// Автоматическая инвалидация после мутаций
const createCarMutation = useMutation(
  (carData: CarCreate) => apiClient.createCar(carData),
  {
    onSuccess: () => {
      queryClient.invalidateQueries('cars');
      queryClient.invalidateQueries('carStatistics');
    },
  }
);
```

### 8.2 Оптимизация запросов

**Ленивая загрузка**:
- Компоненты загружаются по требованию
- Данные кэшируются для повторного использования
- Минимизация количества API запросов

**Пагинация**:
- Ограничение количества записей на странице
- Параметры `skip` и `limit` для API
- Оптимизация для больших объемов данных

---

## 9. ОБРАБОТКА ОШИБОК

### 9.1 Состояния загрузки

**Индикаторы**:
```typescript
{carsLoading ? (
  <div className="flex justify-center py-12">
    <LoadingSpinner size="lg" />
  </div>
) : carsError ? (
  <Card>
    <CardBody className="text-center py-12">
      <div className="text-red-600 mb-4">Ошибка загрузки данных</div>
      <Button onClick={() => queryClient.invalidateQueries('cars')}>
        Попробовать снова
      </Button>
    </CardBody>
  </Card>
) : (
  // Отображение данных
)}
```

### 9.2 Валидация форм

**Клиентская валидация**:
- Обязательные поля помечены атрибутом `required`
- Типы полей проверяются (number, text, email)
- Визуальная обратная связь для пользователя

**Серверная валидация**:
- Pydantic схемы для валидации данных
- Детальные сообщения об ошибках
- HTTP статус коды для различных типов ошибок

---

## 10. ТЕСТИРОВАНИЕ И ОТЛАДКА

### 10.1 Инструменты разработки

**Next.js DevTools**:
- Hot Reload для быстрой разработки
- TypeScript проверки в реальном времени
- ESLint для качества кода

**React Query DevTools**:
- Мониторинг состояния кэша
- Отслеживание запросов и мутаций
- Анализ производительности

### 10.2 Логирование

**API Логирование**:
```typescript
// Request interceptor
this.client.interceptors.request.use(
  (config) => {
    console.log(`🚀 API Request: ${config.method?.toUpperCase()} ${config.url}`);
    return config;
  }
);

// Response interceptor
this.client.interceptors.response.use(
  (response) => {
    console.log(`✅ API Response: ${response.status} ${response.config.url}`);
    return response;
  }
);
```

---

## 11. РЕЗУЛЬТАТЫ И ДОСТИЖЕНИЯ

### 11.1 Реализованный функционал

**✅ Полный CRUD для автомобилей**:
- Создание, чтение, обновление, удаление
- Валидация данных на клиенте и сервере
- Связи с владельцами

**✅ Полный CRUD для владельцев**:
- Управление владельцами
- Каскадное удаление автомобилей
- Статистика по владельцам

**✅ Расширенный поиск**:
- Множественные фильтры
- Сортировка результатов
- Пагинация данных

**✅ Современный UI/UX**:
- Адаптивный дизайн
- Интуитивная навигация
- Быстрые анимации

### 11.2 Технические достижения

**Архитектура**:
- ✅ Модульная структура проекта
- ✅ Типизация с TypeScript
- ✅ Компонентная архитектура React
- ✅ RESTful API с FastAPI

**Производительность**:
- ✅ Кэширование с React Query
- ✅ Оптимизированные запросы
- ✅ Ленивая загрузка компонентов
- ✅ Автоматическое обновление данных

**Качество кода**:
- ✅ ESLint для проверки кода
- ✅ TypeScript для безопасности типов
- ✅ Модульные компоненты
- ✅ Переиспользуемые утилиты

---

## 12. ВОЗМОЖНОСТИ РАСШИРЕНИЯ

### 12.1 Планируемые улучшения

**Функциональность**:
- 🔐 **Аутентификация** - система входа и регистрации
- 📊 **Расширенная аналитика** - графики и диаграммы
- 📱 **PWA** - поддержка офлайн режима
- 🌐 **Интернационализация** - поддержка нескольких языков

**Технические улучшения**:
- 🧪 **Тестирование** - unit и integration тесты
- 🚀 **CI/CD** - автоматизация развертывания
- 📈 **Мониторинг** - отслеживание производительности
- 🔒 **Безопасность** - защита от атак

### 12.2 Масштабирование

**Архитектурные решения**:
- Микросервисная архитектура
- Контейнеризация с Docker
- Балансировка нагрузки
- Кэширование на уровне базы данных

---

## 13. ЗАКЛЮЧЕНИЕ

### 13.1 Достигнутые цели

**Основные цели выполнены**:
- ✅ Создано полнофункциональное веб-приложение
- ✅ Реализован современный пользовательский интерфейс
- ✅ Обеспечена интеграция фронтенда и бэкенда
- ✅ Достигнута высокая производительность
- ✅ Создан удобный и интуитивный интерфейс

### 13.2 Технические навыки

**Полученные компетенции**:
- 🎯 **Next.js** - современный React фреймворк
- 🎨 **Tailwind CSS** - утилитарный CSS фреймворк
- 🔄 **React Query** - управление состоянием сервера
- 📱 **Адаптивный дизайн** - мобильная разработка
- 🔧 **TypeScript** - типизированная разработка
- ⚡ **Оптимизация** - производительность приложений

### 13.3 Практическая ценность

**Результат**:
Создана полнофункциональная система управления автомобилями, демонстрирующая современные подходы к веб-разработке. Приложение готово к использованию в реальных условиях и может служить основой для более сложных проектов.

**Ключевые особенности**:
- 🚀 **Современные технологии** - актуальный стек разработки
- 📱 **Мобильная адаптация** - работа на всех устройствах
- ⚡ **Высокая производительность** - оптимизированные запросы
- 🎨 **Удобный интерфейс** - интуитивная навигация
- 🔧 **Качественный код** - типизация и модульность

---

## 14. ПРИЛОЖЕНИЯ

### 14.1 Структура файлов

```
fullstack-lab-work/
├── app/                    # Backend (FastAPI)
│   ├── main.py            # 325 строк - основное приложение
│   ├── models.py          # 29 строк - SQLAlchemy модели
│   ├── schemas.py         # Pydantic схемы
│   ├── crud.py            # 312 строк - CRUD операции
│   └── db.py              # Подключение к БД
├── components/            # Frontend компоненты
│   ├── cars/              # Компоненты автомобилей
│   ├── owners/            # Компоненты владельцев
│   ├── search/            # Компоненты поиска
│   └── ui/                # UI компоненты
├── pages/                 # Next.js страницы
│   ├── index.tsx          # 272 строки - главная страница
│   ├── cars/simple.tsx    # 530 строк - управление автомобилями
│   ├── owners/simple.tsx  # Управление владельцами
│   └── search/simple.tsx  # Поиск
├── hooks/                 # React хуки
├── lib/                   # Утилиты и API клиент
│   └── api.ts             # 185 строк - API клиент
├── types/                 # TypeScript типы
└── styles/                # Глобальные стили
```

### 14.2 Статистика кода

**Общий объем**:
- **Backend**: ~800 строк Python кода
- **Frontend**: ~1500 строк TypeScript/React кода
- **Компоненты**: 15+ переиспользуемых компонентов
- **Страницы**: 6 основных страниц приложения

**Качество кода**:
- ✅ TypeScript типизация
- ✅ ESLint проверки
- ✅ Модульная архитектура
- ✅ Документированный код

### 14.3 Запуск проекта

**Backend**:
```bash
# Активация виртуального окружения
.\.venv\Scripts\Activate.ps1

# Запуск FastAPI сервера
uvicorn app.main:app --reload --port 8000
```

**Frontend**:
```bash
# Установка зависимостей
npm install

# Запуск Next.js в режиме разработки
npm run dev
```

**Доступ**:
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

---

**Лабораторная работа №3 успешно завершена! 🎉**

Создано современное full-stack веб-приложение с использованием Next.js и FastAPI, демонстрирующее передовые подходы к веб-разработке и готовое к использованию в реальных условиях.
