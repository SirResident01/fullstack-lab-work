# 🔧 Исправление проблемы с переменными окружения на Railway

## ❌ Проблема:

Фронтенд пытается подключиться к `http://127.0.0.1:8000` вместо бэкенда Railway.

**Причина:** В Next.js переменные с префиксом `NEXT_PUBLIC_` встраиваются в код **во время сборки** (build time), а не во время выполнения (runtime).

## ✅ Решение:

### Вариант 1: Установить переменную ДО сборки (рекомендуется)

1. **В Railway Dashboard для Frontend Service:**
   - Перейдите в **Variables**
   - Добавьте переменную:
     - **Key:** `NEXT_PUBLIC_API_URL`
     - **Value:** `https://fullstack-lab-work-123.up.railway.app`
   - **ВАЖНО:** Убедитесь, что переменная установлена **ДО** того, как Railway начнет сборку

2. **Пересоберите проект:**
   - В Railway Dashboard нажмите **Deploy** → **Redeploy**
   - Или сделайте новый commit и push в репозиторий

### Вариант 2: Использовать Build Arguments (если Вариант 1 не работает)

Если Railway не передает переменные окружения в builder stage автоматически:

1. В Railway Dashboard для Frontend Service:
   - Перейдите в **Settings** → **Build**
   - Добавьте Build Argument:
     - **Key:** `NEXT_PUBLIC_API_URL`
     - **Value:** `https://fullstack-lab-work-123.up.railway.app`

2. Пересоберите проект

## 🔍 Проверка:

После пересборки:

1. Откройте фронтенд: https://web-123-b09c.up.railway.app
2. Откройте консоль браузера (F12)
3. Проверьте логи - должны быть запросы к `https://fullstack-lab-work-123.up.railway.app`
4. Проверьте Network tab - все запросы должны идти на правильный URL

## 📝 Важные замечания:

- **NEXT_PUBLIC_*** переменные встраиваются в JavaScript код во время сборки
- После изменения этих переменных **обязательно нужна пересборка**
- Просто перезапуск сервиса не поможет - нужно пересобрать Docker образ
- Railway автоматически пересоберет проект при новом commit/push

## 🚀 Быстрое решение:

1. Установите `NEXT_PUBLIC_API_URL` в Railway Variables
2. Сделайте любой commit и push (например, обновите README)
3. Railway автоматически пересоберет проект с новой переменной

