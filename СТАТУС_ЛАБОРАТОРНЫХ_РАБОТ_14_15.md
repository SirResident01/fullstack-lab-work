# Статус выполнения лабораторных работ 14 и 15

## 📋 Лабораторная работа 14 — Security и аутентификация

### ✅ Выполнено:

1. **✔ Включить Security в backend**
   - ✅ Реализована JWT аутентификация в FastAPI
   - ✅ Используется `HTTPBearer` для защиты эндпоинтов
   - ✅ Функция `get_current_user()` проверяет JWT токен
   - ✅ Функция `role_required()` проверяет роли пользователей
   - ✅ Все CRUD эндпоинты защищены через `Depends(get_current_user)`
   - ✅ Административные операции защищены через `Depends(role_required("ADMIN"))`
   - **Файлы:** `app/main.py` (строки 84-118, 238-704)

2. **✔ Реализовать Login в React**
   - ✅ Создан компонент `LoginForm` (`components/auth/LoginForm.tsx`)
   - ✅ Реализована страница входа (`pages/login.tsx`)
   - ✅ Интегрирован с `AuthContext` для управления состоянием
   - ✅ Поддержка регистрации обычных пользователей и администраторов

3. **✔ Добавлять JWT в CRUD запросы**
   - ✅ Реализован метод `setAuthToken()` в `ApiClient` (`lib/api.ts:217-219`)
   - ✅ Токен автоматически добавляется в заголовок `Authorization: Bearer <token>`
   - ✅ Токен устанавливается после успешного логина
   - ✅ Все API запросы используют токен из заголовков

4. **✔ Logout и очистка токена**
   - ✅ Реализована функция `logout()` в `AuthContext` (`contexts/AuthContext.tsx:121-127`)
   - ✅ Очищается токен из состояния
   - ✅ Очищается токен из `localStorage`
   - ✅ Очищается токен из API клиента через `clearAuthToken()`
   - ✅ Реализован компонент `UserMenu` с кнопкой выхода

### ❌ Не выполнено / Требует исправления:

1. **❌ Хранить JWT в sessionStorage**
   - ❌ **Текущее состояние:** Токен хранится в `localStorage` (`contexts/AuthContext.tsx:18, 53, 124`)
   - ❌ **Требуется:** Изменить на `sessionStorage` для безопасности
   - **Файлы для изменения:**
     - `contexts/AuthContext.tsx` (строки 18, 53, 124)
     - Заменить `localStorage.getItem('auth_token')` → `sessionStorage.getItem('auth_token')`
     - Заменить `localStorage.setItem('auth_token', ...)` → `sessionStorage.setItem('auth_token', ...)`
     - Заменить `localStorage.removeItem('auth_token')` → `sessionStorage.removeItem('auth_token')`

2. **❌ Сделать Snackbar для ошибок**
   - ❌ **Текущее состояние:** Ошибки отображаются через:
     - Простые `<div>` с красным фоном (`components/auth/LoginForm.tsx:57-60`)
     - `alert()` в некоторых местах (`pages/admin/users/index.tsx:117, 134, 136`)
   - ❌ **Требуется:** Реализовать компонент Snackbar (можно использовать MUI `Snackbar` или создать свой)
   - **Файлы для изменения:**
     - Создать компонент `components/ui/Snackbar.tsx` или использовать MUI
     - Добавить контекст для управления уведомлениями
     - Заменить все места с `alert()` и простыми div на Snackbar
     - Основные файлы: `components/auth/LoginForm.tsx`, `pages/admin/users/index.tsx`, `pages/cars/index.tsx`

---

## 📋 Лабораторная работа 15 — Деплой и контейнеризация

### ❌ Не выполнено:

1. **❌ Развернуть БД в AWS RDS**
   - ❌ Нет конфигурации для подключения к AWS RDS
   - ❌ Нет файлов с настройками для AWS
   - ❌ Текущая БД: SQLite (`cardb.db`) или локальная PostgreSQL/SQLite
   - **Требуется:**
     - Создать RDS инстанс в AWS
     - Обновить `app/db.py` для подключения к RDS
     - Добавить переменные окружения для RDS (host, port, database, username, password)
     - Обновить `config.env` с параметрами подключения

2. **❌ Задеплоить backend в Elastic Beanstalk**
   - ❌ Нет конфигурации для Elastic Beanstalk
   - ❌ Нет файла `.ebextensions/` или `application.py` для EB
   - ❌ Нет `requirements.txt` для Python зависимостей (хотя файл есть, но нужно проверить)
   - **Требуется:**
     - Создать файл `.ebextensions/python.config` для настройки Python окружения
     - Создать файл `.elasticbeanstalk/config.yml`
     - Настроить переменные окружения в EB консоли
     - Задеплоить приложение через EB CLI или консоль AWS

3. **❌ Задеплоить frontend в Netlify**
   - ❌ Нет конфигурации для Netlify
   - ❌ Нет файла `netlify.toml`
   - ❌ Нет настроек для Next.js деплоя на Netlify
   - **Требуется:**
     - Создать файл `netlify.toml` с настройками для Next.js
     - Настроить переменные окружения в Netlify (например, `NEXT_PUBLIC_API_BASE_URL`)
     - Подключить репозиторий к Netlify
     - Настроить build команды и publish директорию

4. **❌ Собрать и запустить Docker контейнеры (MariaDB + Spring Boot)**
   - ❌ **Примечание:** В проекте используется FastAPI (Python), а не Spring Boot (Java)
   - ❌ Нет `Dockerfile` для backend
   - ❌ Нет `Dockerfile` для frontend
   - ❌ Нет `docker-compose.yml` для оркестрации контейнеров
   - **Требуется:**
     - Создать `Dockerfile` для FastAPI backend
     - Создать `Dockerfile` для Next.js frontend (или использовать Next.js standalone build)
     - Создать `docker-compose.yml` с сервисами:
       - `mariadb` (или `postgresql` - в зависимости от выбора)
       - `backend` (FastAPI)
       - `frontend` (Next.js) - опционально, можно деплоить отдельно
     - Настроить сеть между контейнерами
     - Настроить volumes для персистентности данных БД

---

## 📊 Итоговая статистика

### Лабораторная работа 14:
- ✅ **Выполнено:** 4 из 6 задач (66.7%)
- ❌ **Требует исправления:** 2 задачи
  - Хранение JWT в sessionStorage
  - Snackbar для ошибок

### Лабораторная работа 15:
- ✅ **Выполнено:** 0 из 4 задач (0%)
- ❌ **Требуется выполнить:** 4 задачи
  - AWS RDS
  - Elastic Beanstalk
  - Netlify
  - Docker контейнеры

---

## 🔧 Рекомендации по приоритетам

1. **Высокий приоритет (Лаба 14):**
   - Изменить `localStorage` на `sessionStorage` для JWT (простое изменение)
   - Реализовать Snackbar для ошибок (улучшение UX)

2. **Средний приоритет (Лаба 15):**
   - Создать Docker контейнеры (можно тестировать локально)
   - Настроить Netlify для frontend (проще всего)

3. **Низкий приоритет (Лаба 15):**
   - Настроить AWS RDS (требует AWS аккаунт и оплату)
   - Настроить Elastic Beanstalk (требует AWS аккаунт и оплату)

---

## 📝 Примечания

- Проект использует **FastAPI** (Python), а не Spring Boot (Java), поэтому в задаче про Docker нужно адаптировать под FastAPI
- Для MariaDB можно использовать PostgreSQL, так как SQLAlchemy поддерживает оба варианта
- Next.js можно деплоить на Netlify, Vercel или другие платформы
- Для тестирования Docker контейнеров можно использовать локальную среду без AWS



