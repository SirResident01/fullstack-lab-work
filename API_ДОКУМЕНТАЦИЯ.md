# 📚 API Документация - Lab #1 FastAPI + PostgreSQL

## 🎯 Обзор

Полная система CRUD операций для управления автомобилями и их владельцами с продвинутыми запросами, фильтрацией и сортировкой.

## 🔗 Базовые эндпоинты

### GET /hello
**Описание**: Простое приветствие  
**Ответ**: `"Hello from FastAPI!"`

### GET /api/status
**Описание**: Статус приложения  
**Ответ**:
```json
{
  "status": "ok",
  "app": "Lab1 FastAPI",
  "version": "1.0.0",
  "timestamp": "2024-01-01T12:00:00"
}
```

## 🚗 CRUD операции для автомобилей

### GET /cars
**Описание**: Получить все автомобили с пагинацией  
**Параметры**:
- `skip` (int, optional): Количество записей для пропуска (по умолчанию: 0)
- `limit` (int, optional): Максимальное количество записей (по умолчанию: 100, максимум: 1000)

**Пример запроса**:
```http
GET /cars?skip=0&limit=10
```

**Ответ**:
```json
[
  {
    "id": 1,
    "brand": "Ford",
    "model": "Mustang",
    "color": "Red",
    "registrationNumber": "ADF-1121",
    "modelYear": 2023,
    "price": 59000,
    "owner_id": 1,
    "owner": "John Johnson",
    "owner_firstname": "John",
    "owner_lastname": "Johnson"
  }
]
```

### GET /cars/{car_id}
**Описание**: Получить автомобиль по ID  
**Параметры**:
- `car_id` (int): ID автомобиля

**Ответ**: Объект CarWithOwner

### POST /cars
**Описание**: Создать новый автомобиль  
**Тело запроса**:
```json
{
  "brand": "Toyota",
  "model": "Camry",
  "color": "Blue",
  "registrationNumber": "ABC-1234",
  "modelYear": 2022,
  "price": 35000,
  "owner_id": 1
}
```

**Ответ**: Объект CarResponse

### PUT /cars/{car_id}
**Описание**: Обновить автомобиль  
**Параметры**:
- `car_id` (int): ID автомобиля

**Тело запроса** (все поля опциональны):
```json
{
  "brand": "Honda",
  "price": 40000
}
```

**Ответ**: Объект CarResponse

### DELETE /cars/{car_id}
**Описание**: Удалить автомобиль  
**Параметры**:
- `car_id` (int): ID автомобиля

**Ответ**:
```json
{
  "message": "Автомобиль успешно удален",
  "success": true
}
```

## 🔍 Продвинутые запросы для автомобилей

### GET /cars/search/brand/{brand}
**Описание**: Найти автомобили по марке (поиск по подстроке)  
**Параметры**:
- `brand` (string): Марка автомобиля

**Пример**:
```http
GET /cars/search/brand/Ford
```

### GET /cars/search/color/{color}
**Описание**: Найти автомобили по цвету (поиск по подстроке)  
**Параметры**:
- `color` (string): Цвет автомобиля

**Пример**:
```http
GET /cars/search/color/Red
```

### GET /cars/search/year/{year}
**Описание**: Найти автомобили по году выпуска  
**Параметры**:
- `year` (int): Год выпуска

**Пример**:
```http
GET /cars/search/year/2023
```

### GET /cars/search/price-range
**Описание**: Найти автомобили в диапазоне цен  
**Параметры**:
- `min_price` (int): Минимальная цена
- `max_price` (int): Максимальная цена

**Пример**:
```http
GET /cars/search/price-range?min_price=30000&max_price=50000
```

### GET /cars/search/owner/{owner_id}
**Описание**: Найти автомобили по владельцу  
**Параметры**:
- `owner_id` (int): ID владельца

**Пример**:
```http
GET /cars/search/owner/1
```

### POST /cars/search
**Описание**: Продвинутый поиск с множественными фильтрами и сортировкой  
**Тело запроса**:
```json
{
  "brand": "Ford",
  "color": "Red",
  "modelYear": 2023,
  "minPrice": 50000,
  "maxPrice": 100000,
  "owner_id": 1,
  "sort_by": "price",
  "sort_order": "desc",
  "limit": 50,
  "offset": 0
}
```

**Параметры фильтрации**:
- `brand` (string, optional): Марка автомобиля
- `color` (string, optional): Цвет автомобиля
- `modelYear` (int, optional): Год выпуска
- `minPrice` (int, optional): Минимальная цена
- `maxPrice` (int, optional): Максимальная цена
- `owner_id` (int, optional): ID владельца

**Параметры сортировки**:
- `sort_by` (string, optional): Поле для сортировки (по умолчанию: "id")
- `sort_order` (string, optional): Порядок сортировки ("asc" или "desc", по умолчанию: "asc")

**Параметры пагинации**:
- `limit` (int, optional): Максимальное количество записей (по умолчанию: 100)
- `offset` (int, optional): Количество записей для пропуска (по умолчанию: 0)

### GET /cars/statistics
**Описание**: Получить статистику по автомобилям  
**Ответ**:
```json
{
  "total_cars": 3,
  "total_owners": 2,
  "average_price": 42333.33,
  "most_expensive": {
    "id": 1,
    "brand": "Ford",
    "model": "Mustang",
    "price": 59000
  },
  "cheapest": {
    "id": 2,
    "brand": "Nissan",
    "model": "Leaf",
    "price": 29000
  }
}
```

## 👥 CRUD операции для владельцев

### GET /owners
**Описание**: Получить всех владельцев с пагинацией  
**Параметры**:
- `skip` (int, optional): Количество записей для пропуска (по умолчанию: 0)
- `limit` (int, optional): Максимальное количество записей (по умолчанию: 100, максимум: 1000)

**Ответ**:
```json
[
  {
    "ownerid": 1,
    "firstname": "John",
    "lastname": "Johnson",
    "cars": [
      {
        "id": 1,
        "brand": "Ford",
        "model": "Mustang",
        "color": "Red",
        "registrationNumber": "ADF-1121",
        "modelYear": 2023,
        "price": 59000,
        "owner_id": 1
      }
    ]
  }
]
```

### GET /owners/{owner_id}
**Описание**: Получить владельца по ID  
**Параметры**:
- `owner_id` (int): ID владельца

**Ответ**: Объект OwnerResponse

### POST /owners
**Описание**: Создать нового владельца  
**Тело запроса**:
```json
{
  "firstname": "Иван",
  "lastname": "Петров"
}
```

**Ответ**: Объект OwnerResponse

### PUT /owners/{owner_id}
**Описание**: Обновить владельца  
**Параметры**:
- `owner_id` (int): ID владельца

**Тело запроса** (все поля опциональны):
```json
{
  "firstname": "Петр",
  "lastname": "Иванов"
}
```

**Ответ**: Объект OwnerResponse

### DELETE /owners/{owner_id}
**Описание**: Удалить владельца (с каскадным удалением всех его автомобилей)  
**Параметры**:
- `owner_id` (int): ID владельца

**Ответ**:
```json
{
  "message": "Владелец и все его автомобили успешно удалены",
  "success": true
}
```

## 🔍 Продвинутые запросы для владельцев

### GET /owners/search/{search_term}
**Описание**: Простой поиск владельцев по любому полю (имя или фамилия)  
**Параметры**:
- `search_term` (string): Термин для поиска в имени или фамилии

**Пример**:
```http
GET /owners/search/John
GET /owners/search/Johnson
GET /owners/search/Jo
```

**Ответ**: Массив объектов OwnerResponse

### POST /owners/search
**Описание**: Продвинутый поиск владельцев с фильтрацией и сортировкой  
**Тело запроса**:
```json
{
  "search": "John",
  "sort_by": "ownerid",
  "sort_order": "asc",
  "limit": 50,
  "offset": 0
}
```

**Параметры фильтрации**:
- `search` (string, optional): Общий поиск по имени ИЛИ фамилии
- `firstname` (string, optional): Точный поиск по имени (если не указан search)
- `lastname` (string, optional): Точный поиск по фамилии (если не указан search)

**Параметры сортировки**:
- `sort_by` (string, optional): Поле для сортировки (по умолчанию: "ownerid")
- `sort_order` (string, optional): Порядок сортировки ("asc" или "desc", по умолчанию: "asc")

**Параметры пагинации**:
- `limit` (int, optional): Максимальное количество записей (по умолчанию: 100)
- `offset` (int, optional): Количество записей для пропуска (по умолчанию: 0)

**Примечание**: Если указан параметр `search`, то `firstname` и `lastname` игнорируются.

### GET /owners/statistics
**Описание**: Получить статистику по владельцам с количеством автомобилей  
**Ответ**:
```json
[
  {
    "ownerid": 1,
    "firstname": "John",
    "lastname": "Johnson",
    "car_count": 1
  },
  {
    "ownerid": 2,
    "firstname": "Mary",
    "lastname": "Robinson",
    "car_count": 2
  }
]
```

## 📊 Схемы данных

### CarWithOwner
```json
{
  "id": 1,
  "brand": "Ford",
  "model": "Mustang",
  "color": "Red",
  "registrationNumber": "ADF-1121",
  "modelYear": 2023,
  "price": 59000,
  "owner_id": 1,
  "owner": "John Johnson",
  "owner_firstname": "John",
  "owner_lastname": "Johnson"
}
```

### CarCreate
```json
{
  "brand": "Toyota",
  "model": "Camry",
  "color": "Blue",
  "registrationNumber": "ABC-1234",
  "modelYear": 2022,
  "price": 35000,
  "owner_id": 1
}
```

### CarUpdate
```json
{
  "brand": "Honda",
  "price": 40000
}
```

### OwnerResponse
```json
{
  "ownerid": 1,
  "firstname": "John",
  "lastname": "Johnson",
  "cars": [
    {
      "id": 1,
      "brand": "Ford",
      "model": "Mustang",
      "color": "Red",
      "registrationNumber": "ADF-1121",
      "modelYear": 2023,
      "price": 59000,
      "owner_id": 1
    }
  ]
}
```

### OwnerCreate
```json
{
  "firstname": "Иван",
  "lastname": "Петров"
}
```

## 🚀 Примеры использования

### Создание владельца и автомобиля
```bash
# 1. Создать владельца
curl -X POST "http://localhost:8000/owners" \
  -H "Content-Type: application/json" \
  -d '{"firstname": "Иван", "lastname": "Петров"}'

# 2. Создать автомобиль для владельца
curl -X POST "http://localhost:8000/cars" \
  -H "Content-Type: application/json" \
  -d '{
    "brand": "Toyota",
    "model": "Camry",
    "color": "Blue",
    "registrationNumber": "ABC-1234",
    "modelYear": 2022,
    "price": 35000,
    "owner_id": 1
  }'
```

### Поиск автомобилей
```bash
# Поиск по марке
curl "http://localhost:8000/cars/search/brand/Ford"

# Поиск по цвету
curl "http://localhost:8000/cars/search/color/Red"

# Поиск по году
curl "http://localhost:8000/cars/search/year/2023"

# Поиск по диапазону цен
curl "http://localhost:8000/cars/search/price-range?min_price=30000&max_price=50000"
```

### Продвинутый поиск автомобилей
```bash
curl -X POST "http://localhost:8000/cars/search" \
  -H "Content-Type: application/json" \
  -d '{
    "brand": "Ford",
    "color": "Red",
    "minPrice": 50000,
    "sort_by": "price",
    "sort_order": "desc",
    "limit": 10
  }'
```

### Поиск владельцев
```bash
# Простой поиск по любому полю
curl "http://localhost:8000/owners/search/John"
curl "http://localhost:8000/owners/search/Johnson"
curl "http://localhost:8000/owners/search/Jo"

# Продвинутый поиск владельцев
curl -X POST "http://localhost:8000/owners/search" \
  -H "Content-Type: application/json" \
  -d '{
    "search": "John",
    "sort_by": "ownerid",
    "sort_order": "asc",
    "limit": 10
  }'
```

## 🔧 Коды ошибок

- **200 OK**: Успешный запрос
- **201 Created**: Ресурс успешно создан
- **400 Bad Request**: Неверные параметры запроса
- **404 Not Found**: Ресурс не найден
- **422 Unprocessable Entity**: Ошибка валидации данных
- **500 Internal Server Error**: Внутренняя ошибка сервера

## 📝 Примечания

1. **Пагинация**: Все эндпоинты списков поддерживают пагинацию через параметры `skip` и `limit`
2. **Сортировка**: Продвинутые запросы поддерживают сортировку по любому полю
3. **Фильтрация**: Поиск по строкам использует `ILIKE` для регистронезависимого поиска
4. **Каскадное удаление**: При удалении владельца автоматически удаляются все его автомобили
5. **Валидация**: Все входные данные валидируются через Pydantic модели
6. **Логирование**: Все операции логируются с уровнем DEBUG
