# 🐳 Инструкция по запуску проекта в Docker

## 📋 Предварительные требования

1. **Установленный Docker Desktop** (для Windows)
   - Скачать с [официального сайта](https://www.docker.com/products/docker-desktop/)
   - Убедитесь, что Docker Desktop запущен

2. **Проверка установки Docker:**
```bash
docker --version
docker-compose --version
```

## 🚀 Быстрый старт

### Шаг 1: Создайте файл `.env` в корне проекта

Создайте файл `.env` со следующим содержимым (или используйте значения по умолчанию из docker-compose.yml):

```env
# База данных MariaDB
DB_ROOT_PASSWORD=rootpassword
DB_NAME=cardb
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=3306

# Backend (FastAPI)
BACKEND_PORT=8000
SECRET_KEY=your-super-secret-key-change-in-production-12345
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Frontend (Next.js)
FRONTEND_PORT=3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```

**Важно:** Файл `.env` уже добавлен в `.dockerignore`, поэтому он не будет скопирован в образы.

### Шаг 2: Запустите все сервисы

```bash
docker-compose up -d
```

Эта команда:
- Соберет образы для backend и frontend
- Запустит MariaDB базу данных
- Запустит FastAPI backend
- Запустит Next.js frontend
- Создаст необходимые сети и volumes

### Шаг 3: Проверьте статус контейнеров

```bash
docker-compose ps
```

Вы должны увидеть три запущенных контейнера:
- `car-db` (MariaDB)
- `car-backend` (FastAPI)
- `car-frontend` (Next.js)

### Шаг 4: Откройте приложение в браузере

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8000
- **API Документация:** http://localhost:8000/docs

## 📊 Полезные команды

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend

# Только база данных
docker-compose logs -f mariadb
```

### Остановка контейнеров

```bash
# Остановить без удаления
docker-compose stop

# Остановить и удалить контейнеры
docker-compose down

# Остановить и удалить контейнеры + volumes (удалит данные БД!)
docker-compose down -v
```

### Пересборка образов

```bash
# Пересобрать все образы
docker-compose build

# Пересобрать без кэша
docker-compose build --no-cache

# Пересобрать и запустить
docker-compose up -d --build
```

### Выполнение команд внутри контейнера

```bash
# Войти в контейнер backend
docker-compose exec backend bash

# Войти в контейнер frontend
docker-compose exec frontend sh

# Выполнить команду в контейнере
docker-compose exec backend python -m pip list
```

## 🔧 Настройка

### Изменение портов

Если порты 3000, 8000 или 3306 заняты, измените их в файле `.env`:

```env
FRONTEND_PORT=3001
BACKEND_PORT=8001
DB_PORT=3307
```

### Изменение паролей БД

Измените в файле `.env`:

```env
DB_ROOT_PASSWORD=новый_пароль_root
DB_PASSWORD=новый_пароль_пользователя
```

**Важно:** После изменения паролей БД нужно пересоздать volumes:

```bash
docker-compose down -v
docker-compose up -d
```

### Переменные окружения для Backend

Backend получает переменные из `docker-compose.yml`:
- `DB_URL` - автоматически формируется из переменных БД
- `SECRET_KEY` - секретный ключ для JWT
- `ALGORITHM` - алгоритм шифрования JWT
- `ACCESS_TOKEN_EXPIRE_MINUTES` - время жизни токена

### Переменные окружения для Frontend

Frontend получает:
- `NEXT_PUBLIC_API_BASE_URL` - URL backend API
- `NODE_ENV=production` - режим production

## 🐛 Решение проблем

### Проблема: Контейнеры не запускаются

**Решение:**
1. Проверьте, что Docker Desktop запущен
2. Проверьте логи: `docker-compose logs`
3. Убедитесь, что порты не заняты другими приложениями

### Проблема: Backend не может подключиться к БД

**Решение:**
1. Убедитесь, что MariaDB контейнер запущен: `docker-compose ps`
2. Проверьте логи БД: `docker-compose logs mariadb`
3. Проверьте, что переменные окружения в `.env` правильные
4. Дождитесь готовности БД (healthcheck может занять время)

### Проблема: Frontend не может подключиться к Backend

**Решение:**
1. Проверьте, что backend запущен: `docker-compose ps`
2. Проверьте переменную `NEXT_PUBLIC_API_BASE_URL` в `.env`
3. Убедитесь, что backend доступен: `curl http://localhost:8000/api/status`

### Проблема: Изменения в коде не применяются

**Решение:**
- Backend имеет volume mount для автоматической перезагрузки
- Frontend нужно пересобрать: `docker-compose up -d --build frontend`

### Проблема: Ошибка при сборке Frontend

**Решение:**
1. Убедитесь, что `next.config.js` содержит `output: 'standalone'`
2. Очистите кэш: `docker-compose build --no-cache frontend`
3. Проверьте логи сборки: `docker-compose logs frontend`

## 📁 Структура Docker файлов

```
.
├── docker-compose.yml      # Оркестрация всех сервисов
├── Dockerfile.backend      # Образ для FastAPI
├── Dockerfile.frontend     # Образ для Next.js
├── .dockerignore          # Исключения для Docker
└── .env                   # Переменные окружения (создать вручную)
```

## 🔐 Безопасность

⚠️ **Важно для production:**

1. **Измените все пароли по умолчанию**
2. **Используйте сильный SECRET_KEY** (минимум 32 символа)
3. **Не коммитьте `.env` файл в Git**
4. **Используйте secrets management** для production
5. **Настройте firewall** для ограничения доступа к портам

## 📚 Дополнительная информация

- [Docker Compose документация](https://docs.docker.com/compose/)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Next.js документация](https://nextjs.org/docs)

## ✅ Проверка работоспособности

После запуска проверьте:

1. **База данных:**
```bash
docker-compose exec mariadb mysql -u postgres -ppostgres -e "SHOW DATABASES;"
```

2. **Backend API:**
```bash
curl http://localhost:8000/api/status
```

3. **Frontend:**
   - Откройте http://localhost:3000 в браузере
   - Должна загрузиться главная страница

---

**Готово!** Ваше приложение должно работать в Docker. 🎉


