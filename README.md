# FastAPI Car Management with JWT Authentication

Полнофункциональное FastAPI приложение с JWT аутентификацией и ролевой системой доступа (RBAC) для управления автомобилями.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/Mac)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка базы данных

1. **Создайте базу данных в PostgreSQL:**
   ```sql
   CREATE DATABASE car_auth_db;
   ```

2. **Настройте переменные окружения:**
   ```bash
   # Скопируйте файл конфигурации
   cp env.example .env
   
   # Отредактируйте .env файл с вашими настройками
   DATABASE_URL=postgresql+psycopg://postgres:your_password@localhost:5432/car_auth_db
   SECRET_KEY=your-super-secret-key-change-in-production
   ```

### 3. Создание первого администратора (опционально)

```bash
# Создание первого администратора
python create_admin.py
```

### 4. Запуск приложения

```bash
# Запуск сервера
python run.py

# Или через uvicorn напрямую
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

**Приложение будет доступно по адресу:** http://127.0.0.1:8000

## 📚 API Документация

- **Swagger UI:** http://127.0.0.1:8000/docs
- **ReDoc:** http://127.0.0.1:8000/redoc

## 🔐 Аутентификация

### Роли пользователей

- **USER** - обычный пользователь (может просматривать автомобили)
- **ADMIN** - администратор (полный доступ ко всем операциям)

### Система аутентификации

**Регистрация пользователей:**
- Новые пользователи регистрируются через веб-интерфейс
- Все новые пользователи получают роль **USER** по умолчанию
- Для получения роли **ADMIN** используйте скрипт `create_admin.py`

**Роли пользователей:**
| Role  | Доступ |
|-------|--------|
| **USER**  | Просмотр автомобилей и владельцев |
| **ADMIN** | Полный доступ (создание, редактирование, удаление) |

### API Примеры

**Регистрация нового пользователя:**
```bash
curl -X POST "http://127.0.0.1:8000/register" \
  -H "Content-Type: application/json" \
  -d '{"username": "newuser", "password": "password123", "confirm_password": "password123"}'
```

**Вход в систему:**
```bash
curl -X POST "http://127.0.0.1:8000/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "newuser", "password": "password123"}'
```

**Ответ:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

## 🛠️ API Эндпоинты

### Аутентификация

| Метод | Эндпоинт | Описание | Доступ |
|-------|----------|----------|--------|
| POST | `/login` | Получение JWT токена | Публичный |
| GET | `/users/me` | Информация о текущем пользователе | Аутентифицированный |

### Управление автомобилями

| Метод | Эндпоинт | Описание | Доступ |
|-------|----------|----------|--------|
| GET | `/cars` | Список всех автомобилей | USER, ADMIN |
| GET | `/cars/{id}` | Получить автомобиль по ID | USER, ADMIN |
| POST | `/cars` | Создать новый автомобиль | ADMIN |
| PUT | `/cars/{id}` | Обновить автомобиль | ADMIN |
| DELETE | `/cars/{id}` | Удалить автомобиль | ADMIN |

### Административные

| Метод | Эндпоинт | Описание | Доступ |
|-------|----------|----------|--------|
| GET | `/admin/secret` | Секретная информация | ADMIN |

## 🧪 Тест-сценарии

### 1. Успешная аутентификация

```bash
# Логин пользователя
curl -X POST "http://127.0.0.1:8000/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "user", "password": "user"}'
```

### 2. Доступ к защищенным ресурсам

```bash
# Получение списка автомобилей (требует токен)
curl -X GET "http://127.0.0.1:8000/cars" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

### 3. Проверка ролей

```bash
# Создание автомобиля с токеном USER (должно вернуть 403)
curl -X POST "http://127.0.0.1:8000/cars" \
  -H "Authorization: Bearer USER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"brand": "Tesla", "model": "Model S", "color": "White", "year": 2023, "price": 80000}'

# Создание автомобиля с токеном ADMIN (должно успешно)
curl -X POST "http://127.0.0.1:8000/cars" \
  -H "Authorization: Bearer ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"brand": "Tesla", "model": "Model S", "color": "White", "year": 2023, "price": 80000}'
```

### 4. Обработка ошибок

```bash
# Запрос без токена (должно вернуть 401)
curl -X GET "http://127.0.0.1:8000/cars"

# Неверные учетные данные (должно вернуть 401)
curl -X POST "http://127.0.0.1:8000/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "user", "password": "wrong_password"}'
```

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|-----------|--------------|
| `DATABASE_URL` | URL подключения к PostgreSQL | `postgresql+psycopg://postgres:password@localhost:5432/car_auth_db` |
| `SECRET_KEY` | Секретный ключ для JWT | `your-super-secret-key-change-in-production` |
| `ALGORITHM` | Алгоритм шифрования JWT | `HS256` |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Время жизни токена (минуты) | `1440` (24 часа) |
| `CORS_ORIGINS` | Разрешенные CORS origins | `http://localhost:3000,http://localhost:8080` |

### Настройка PostgreSQL

1. Установите PostgreSQL
2. Создайте базу данных:
   ```sql
   CREATE DATABASE car_auth_db;
   CREATE USER car_user WITH PASSWORD 'your_password';
   GRANT ALL PRIVILEGES ON DATABASE car_auth_db TO car_user;
   ```
3. Обновите `DATABASE_URL` в `.env` файле

## 🏗️ Архитектура

### Структура проекта

```
auth_app/
├── __init__.py          # Инициализация пакета
├── main.py              # Основное приложение FastAPI
├── database.py          # Конфигурация базы данных
├── models.py            # SQLAlchemy модели
├── schemas.py           # Pydantic схемы
├── auth.py              # JWT аутентификация и авторизация
└── crud.py              # CRUD операции
```

### Технологический стек

- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **JWT** - JSON Web Tokens для аутентификации
- **Passlib** - хэширование паролей
- **Pydantic** - валидация данных

## 🔒 Безопасность

### Хэширование паролей

- Используется **bcrypt** для хэширования паролей
- Пароли никогда не хранятся в открытом виде
- Соль генерируется автоматически

### JWT Токены

- **Алгоритм:** HS256
- **Время жизни:** 24 часа (настраивается)
- **Секретный ключ:** настраивается через переменные окружения

### CORS

- Настроен для работы с фронтендом
- Поддержка preflight запросов
- Настраиваемые origins

## 🚀 Развертывание

### Локальная разработка

```bash
# Клонирование репозитория
git clone <repository-url>
cd fastapi-jwt-auth

# Установка зависимостей
pip install -r requirements.txt

# Настройка окружения
cp env.example .env
# Отредактируйте .env файл

# Запуск
python run.py
```

### Продакшн

1. **Используйте сильный SECRET_KEY**
2. **Настройте HTTPS**
3. **Используйте переменные окружения для конфигурации**
4. **Настройте мониторинг и логирование**

## 📝 Логи и отладка

### Включение SQL логирования

В `database.py` установите `echo=True` для SQLAlchemy:

```python
engine = create_engine(DATABASE_URL, echo=True)
```

### Логирование FastAPI

```bash
# Запуск с подробными логами
uvicorn auth_app.main:app --reload --log-level debug
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи приложения
2. Убедитесь, что PostgreSQL запущен
3. Проверьте настройки в `.env` файле
4. Создайте issue в репозитории

---

**Готово к использованию! 🎉**