# 🔐 Система управления автомобилями с JWT аутентификацией

Полнофункциональная система управления автомобилями с современной JWT аутентификацией и ролевой системой доступа (RBAC).

## 🚀 Быстрый запуск

### 1. Установка зависимостей

```bash
# Backend (Python)
pip install -r requirements.txt

# Frontend (Node.js)
npm install
```

### 2. Настройка базы данных

1. **Создайте базу данных в PostgreSQL:**
   ```sql
   CREATE DATABASE cardb;
   ```

2. **Настройте переменные окружения:**
   ```bash
   # Создайте .env файл
   DB_URL=postgresql+psycopg://postgres:your_password@localhost:5432/cardb
   SECRET_KEY=your-super-secret-key-change-in-production
   ALGORITHM=HS256
   ACCESS_TOKEN_EXPIRE_MINUTES=1440
   ```

### 3. Запуск системы

**Backend:**
```bash
# Запуск FastAPI сервера
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

**Frontend:**
```bash
# Запуск Next.js приложения
npm run dev
```

**Доступ:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

## 🔐 Аутентификация

### Тестовые пользователи

При первом запуске автоматически создаются:

| Username | Password | Role  | Доступ |
|----------|----------|-------|--------|
| user     | user     | USER  | Просмотр автомобилей и владельцев |
| admin    | admin    | ADMIN | Полный доступ ко всем функциям |

### Роли и права доступа

**USER (Обычный пользователь):**
- ✅ Просмотр списка автомобилей
- ✅ Просмотр списка владельцев
- ✅ Поиск и фильтрация
- ✅ Просмотр статистики
- ❌ Создание/редактирование/удаление (только просмотр)

**ADMIN (Администратор):**
- ✅ Все права USER
- ✅ Создание новых автомобилей
- ✅ Редактирование автомобилей
- ✅ Удаление автомобилей
- ✅ Создание новых владельцев
- ✅ Редактирование владельцев
- ✅ Удаление владельцев
- ✅ Доступ к админ панели

## 🎯 Функциональность

### 🔐 Система аутентификации

- **JWT токены** с настраиваемым временем жизни
- **Хэширование паролей** с bcrypt
- **Ролевая система** (USER/ADMIN)
- **Автоматическое перенаправление** на страницу входа
- **Защищенные маршруты** с проверкой прав доступа

### 🚗 Управление автомобилями

- **Просмотр списка** - доступно всем авторизованным пользователям
- **Создание** - только для администраторов
- **Редактирование** - только для администраторов
- **Удаление** - только для администраторов
- **Поиск и фильтрация** - доступно всем

### 👥 Управление владельцами

- **Просмотр списка** - доступно всем авторизованным пользователям
- **CRUD операции** - только для администраторов
- **Статистика** - количество автомобилей у каждого владельца

### 🛡️ Админ панель

- **Доступ только для администраторов**
- **Управление всеми ресурсами**
- **Статистика и аналитика**
- **Настройки системы**

## 📱 Пользовательский интерфейс

### Страницы

- **`/login`** - Страница входа в систему
- **`/`** - Главная страница (требует аутентификации)
- **`/cars/simple`** - Управление автомобилями
- **`/owners/simple`** - Управление владельцами
- **`/search/simple`** - Расширенный поиск
- **`/admin`** - Админ панель (только для администраторов)

### Компоненты аутентификации

- **`LoginForm`** - Форма входа в систему
- **`UserMenu`** - Меню пользователя с выходом
- **`ProtectedRoute`** - Защищенные маршруты
- **`AuthContext`** - Контекст аутентификации

## 🔧 Технические особенности

### Backend (FastAPI)

- **JWT аутентификация** с python-jose
- **Хэширование паролей** с passlib[bcrypt]
- **Ролевая система** с декораторами
- **CORS настройки** для фронтенда
- **Автоматическая инициализация** пользователей

### Frontend (Next.js + React)

- **React Context** для управления состоянием аутентификации
- **Защищенные маршруты** с проверкой ролей
- **Автоматическое перенаправление** на страницу входа
- **Сохранение токена** в localStorage
- **Автоматическое обновление** токена

## 🧪 Тестирование

### Тест-сценарии

1. **Вход в систему:**
   - Перейдите на http://localhost:3000
   - Вас перенаправит на страницу входа
   - Войдите как `user/user` или `admin/admin`

2. **Проверка ролей:**
   - Войдите как `user` - увидите только кнопки просмотра
   - Войдите как `admin` - увидите все кнопки управления

3. **Защищенные маршруты:**
   - Попробуйте перейти на `/admin` как `user` - получите ошибку доступа
   - Перейдите на `/admin` как `admin` - получите доступ к панели

### API тестирование

```bash
# Получение токена
curl -X POST "http://localhost:8000/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin"}'

# Использование токена
curl -X GET "http://localhost:8000/cars" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

## 🔒 Безопасность

### Реализованные меры

- **JWT токены** с подписью и временем жизни
- **Хэширование паролей** с bcrypt и солью
- **Ролевая система** с проверкой на уровне API
- **CORS настройки** для безопасности
- **Валидация данных** с Pydantic
- **Защита от SQL-инъекций** с SQLAlchemy

### Рекомендации для продакшна

- Используйте сильный `SECRET_KEY`
- Настройте HTTPS
- Ограничьте время жизни токенов
- Добавьте rate limiting
- Настройте мониторинг и логирование

## 📊 Мониторинг и отладка

### Логирование

- **Backend:** Все операции логируются с уровнем DEBUG
- **Frontend:** Консольные логи для отладки
- **API:** Детальное логирование запросов и ответов

### Отладка

```bash
# Backend логи
uvicorn app.main:app --reload --log-level debug

# Frontend DevTools
# Откройте DevTools в браузере для просмотра логов
```

## 🚀 Развертывание

### Локальная разработка

```bash
# 1. Клонирование репозитория
git clone <repository-url>
cd fullstack-lab-work

# 2. Backend
pip install -r requirements.txt
uvicorn app.main:app --reload

# 3. Frontend
npm install
npm run dev
```

### Продакшн

1. **Настройте переменные окружения**
2. **Используйте сильные секретные ключи**
3. **Настройте HTTPS**
4. **Используйте reverse proxy (nginx)**
5. **Настройте мониторинг**

## 📚 Дополнительные ресурсы

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Next.js Documentation](https://nextjs.org/docs)
- [JWT.io](https://jwt.io/) - Отладка JWT токенов
- [React Context API](https://reactjs.org/docs/context.html)

---

**Система готова к использованию! 🎉**

Создана полнофункциональная система с современной аутентификацией, готовая для демонстрации и дальнейшего развития.

