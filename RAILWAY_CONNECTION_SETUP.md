# 🔗 Подключение фронтенда и бэкенда на Railway

## 📍 Текущие URL:

- **Фронтенд:** https://web-123-b09c.up.railway.app
- **Бэкенд:** https://fullstack-lab-work-123.up.railway.app

## ✅ Что уже сделано:

1. ✅ Обновлен CORS на бэкенде - добавлен домен фронтенда Railway
2. ✅ Фронтенд настроен на использование `NEXT_PUBLIC_API_URL`

## 🔧 Что нужно сделать на Railway:

### 1. Настройка переменных окружения для фронтенда:

1. Откройте ваш **Frontend Service** в Railway Dashboard
2. Перейдите в раздел **Variables**
3. Добавьте переменную:
   - **Key:** `NEXT_PUBLIC_API_URL`
   - **Value:** `https://fullstack-lab-work-123.up.railway.app`
4. Сохраните изменения
5. Railway автоматически перезапустит сервис

### 2. Проверка CORS на бэкенде (опционально):

Если хотите использовать переменную окружения вместо хардкода:

1. Откройте ваш **Backend Service** в Railway Dashboard
2. Перейдите в раздел **Variables**
3. Добавьте переменную:
   - **Key:** `CORS_ORIGINS`
   - **Value:** `https://web-123-b09c.up.railway.app,https://fullstack-lab-work.vercel.app`
4. Сохраните изменения

**Примечание:** CORS уже настроен в коде, но использование переменной окружения более гибко.

## 🧪 Проверка подключения:

После настройки переменных окружения:

1. Откройте фронтенд: https://web-123-b09c.up.railway.app
2. Откройте консоль браузера (F12)
3. Проверьте, что запросы идут на правильный бэкенд:
   - Должны быть запросы к `https://fullstack-lab-work-123.up.railway.app`
4. Проверьте, что нет ошибок CORS

## 🔍 Отладка:

Если возникают проблемы:

1. **CORS ошибки:**
   - Проверьте, что домен фронтенда добавлен в CORS на бэкенде
   - Проверьте логи бэкенда в Railway Dashboard

2. **404 ошибки:**
   - Убедитесь, что `NEXT_PUBLIC_API_URL` установлен правильно
   - Проверьте, что бэкенд доступен по указанному URL

3. **Проверка переменных окружения:**
   - В Railway Dashboard → Variables проверьте наличие `NEXT_PUBLIC_API_URL`
   - Убедитесь, что значение правильное (без пробелов, с https://)

## 📝 Примечания:

- Переменные окружения с префиксом `NEXT_PUBLIC_` доступны в браузере
- После изменения переменных окружения Railway автоматически перезапускает сервис
- Изменения вступают в силу через 1-2 минуты после сохранения

